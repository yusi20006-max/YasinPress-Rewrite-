# Operations

Configure runtime values with `.env`, JSON, YAML, or environment variables. SQLite databases live under `data/` by default. Use the backup helper before migrations in production and monitor health, diagnostics, and metrics snapshots.

# Developer Guide

Install with `pip install -e .[dev]`, run checks with `pytest`, and use `yasinpress status` to verify the CLI. Keep modules typed, focused, documented, and free of framework coupling. Add adapters at package edges and keep domain behavior deterministic where possible.
